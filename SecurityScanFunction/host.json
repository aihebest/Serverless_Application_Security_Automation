{
  "version": "2.0",
  "logging": {
    "applicationInsights": {
      "samplingSettings": {
        "isEnabled": true,
        "excludedTypes": "Request"
      }
    }
  }
  // Remove or comment out the "extensionBundle" section if it exists
  // "extensionBundle": {
  //   "id": "Microsoft.Azure.Functions.ExtensionBundle",
  //   "version": "[2.*, 3.0.0)"
  // }
}